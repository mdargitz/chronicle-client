//reusable list element for characters, settings and plots

import React from 'react';
import {connect} from 'react-redux';

export class DetailList extends React.Component {
//give cards their detailId by maping through array of characters in state
}

export default connect()(DetailList);